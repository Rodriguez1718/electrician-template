---
import Hero2 from "../components/section/Hero2.astro";
import ServicesList from "../components/section/ServicesList.astro";
import BaseLayout from "../layouts/BaseLayout.astro";
import CommercialOffers from "../components/section/CommercialOffers.astro";
import FAQ from "../components/section/FAQ.astro";
import CallToAction from "../components/section/CallToAction.astro";
import ServiceCoverage from "../components/section/ServiceCoverage.astro";
import CommercialServices from "../components/section/CommercialServices.astro";
import Services from "../components/section/Services.astro";

---

<BaseLayout 
	title="Residential Electrical Services | VoltBully Professional Electricians"
	description="VoltBully offers comprehensive residential electrical services including home rewiring, GFCI outlets, ceiling fans, backup generators, and EV charging stations. Licensed residential electricians with emergency service."
	keywords="residential electrician, home electrical services, house rewiring, GFCI outlets, ceiling fan installation, backup generators, EV charging stations, home electrical contractor"
>
    <div class="relative">
        <!-- Background image for Hero2 -->
        <div class="absolute inset-0 bg-cover bg-center bg-no-repeat" style="background-image: url('/images/residential.webp');"></div>
        <!-- Dark overlay for better text readability -->
        <div class="absolute inset-0 bg-black/40"></div>
        
        <Hero2 
            line1="RESIDENTIAL"
            line2="ELECTRICAL"
            line3="SERVICES"
            description="Professional home electrical services for families and homeowners."
            buttonText="GET STARTED"
            buttonHref="/contact"
            phoneText="CALL FOR EMERGENCY"
        />
    </div>
    <CommercialServices 
        sectionTag="RESIDENTIAL SERVICES"
        title="Expert Home Electrical Solutions"
        highlightedText="Home Electrical"
        description="Our certified residential electricians provide safe, reliable electrical services for your home and family. From simple repairs to complete rewiring, we ensure your home's electrical system meets the highest safety standards."
        companyName="VoltBully Electrical"
        image={{
            src: "/images/rewiring.webp",
            alt: "Residential electrical services"
        }}
        badge={{
            number: "15+",
            text: ["Years", "Experience"]
        }}
        features={[
            {
            title: "Safety First",
            description: "We prioritize your family's safety with code-compliant installations and thorough inspections.",
            icon: '<svg width="24" height="24" viewBox="0 0 24 24" fill="none"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>',
            color: 'primary'
            },
            {
            title: "Licensed Experts",
            description: "All our residential electricians are fully licensed, insured, and background checked.",
            icon: '<svg width="24" height="24" viewBox="0 0 24 24" fill="none"><path d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z" stroke="currentColor" stroke-width="2"/></svg>',
            color: 'secondary'
            },
            {
            title: "Modern Solutions",
            description: "Smart home integration, EV charging stations, and energy-efficient upgrades.",
            icon: '<svg width="24" height="24" viewBox="0 0 24 24" fill="none"><path d="M13 2L3 14h9l-1 8 10-12h-9l1-8z" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>',
            color: 'accent'
            },
            {
            title: "Fair Pricing",
            description: "Transparent, upfront pricing with no hidden fees or surprise charges.",
            icon: '<svg width="24" height="24" viewBox="0 0 24 24" fill="none"><path d="M12 2v20M17 5H9.5a3.5 3.5 0 0 0 0 7h5a3.5 3.5 0 0 1 0 7H6" stroke="currentColor" stroke-width="2"/></svg>',
            color: 'red'
            }
        ]}
    />

    <ServicesList 
    category="residential"
    sectionLabel="Residential Services"
    title="Expert"
    titleHighlight="Home Electrical"
    description="Professional electrical services designed specifically for residential properties. From simple repairs to complete home rewiring, we provide safe, reliable electrical solutions that protect your family and enhance your home's functionality."
    />

    <!-- Residential services -->
    <!-- <ServicesList 
    category="residential"
    sectionLabel="Residential Services"
    title="Expert"
    titleHighlight="Home Electrical"
    description="Professional electrical services for your home and family's safety."
    /> -->

    <!-- Emergency services -->
    <!-- <ServicesList 
    category="emergency"
    sectionLabel="Emergency Services"
    title="24/7"
    titleHighlight="Emergency Response"
    description="Round-the-clock electrical emergency services when you need them most."
    /> -->

    <!-- Specialty services -->
    <!-- <ServicesList 
    category="specialty"
    sectionLabel="Specialty Services"
    title="Premium"
    titleHighlight="Specialty Solutions"
    description="Specialized electrical services for unique projects and installations."
    /> -->
    <CommercialOffers />
    <FAQ />
    <Services excludeCategory="residential" />
    <CallToAction variant="light" />
    <ServiceCoverage />
    
    <!-- Back to Top Button -->
    <button 
        id="backToTop" 
        class="fixed bottom-8 right-8 w-12 h-12 bg-brand-primary text-black rounded-full shadow-lg hover:shadow-xl transition-all duration-300 opacity-0 invisible cursor-none overflow-hidden"
        style="z-index: 9999; position: fixed;"
        aria-label="Back to top"
        type="button"
    >
        <svg width="24" height="24" viewBox="0 0 24 24" fill="none" class="mx-auto">
            <path d="M12 19V5M5 12l7-7 7 7" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
        </svg>
    </button>
</BaseLayout>

<style>
    #backToTop::before {
        content: '';
        position: absolute;
        width: 0;
        height: 0;
        background: var(--color-secondary);
        border-radius: 50%;
        transform: translate(-50%, -50%);
        transition: width 0.4s ease, height 0.4s ease;
        z-index: 1;
        pointer-events: none;
        left: var(--mouse-x, 50%);
        top: var(--mouse-y, 50%);
    }
    
    #backToTop:hover::before {
        width: 300%;
        height: 300%;
    }
    
    #backToTop:hover {
        color: white;
    }
    
    #backToTop.btn--shrinking::before {
        width: 0;
        height: 0;
    }
    
    #backToTop.btn--shrinking {
        color: black;
    }
    
    #backToTop svg {
        position: relative;
        z-index: 2;
        pointer-events: none;
    }
</style>

<script>
    // Back to top button functionality
    const backToTopButton = document.getElementById('backToTop');
    
    // Show/hide button based on scroll position
    window.addEventListener('scroll', () => {
        if (window.scrollY > 300) {
            backToTopButton?.classList.remove('opacity-0', 'invisible');
            backToTopButton?.classList.add('opacity-100', 'visible');
        } else {
            backToTopButton?.classList.add('opacity-0', 'invisible');
            backToTopButton?.classList.remove('opacity-100', 'visible');
        }
    });
    
    // Smooth scroll to top when clicked
    backToTopButton?.addEventListener('click', () => {
        window.scrollTo({
            top: 0,
            behavior: 'smooth'
        });
    });
    
    // Mouse tracking for hover effect (same as Button component)
    if (backToTopButton) {
        backToTopButton.addEventListener('mouseenter', (e) => {
            const mouseEvent = e as MouseEvent;
            const rect = backToTopButton.getBoundingClientRect();
            const x = mouseEvent.clientX - rect.left;
            const y = mouseEvent.clientY - rect.top;
            
            // Set CSS custom properties for the circle position
            backToTopButton.style.setProperty('--mouse-x', `${x}px`);
            backToTopButton.style.setProperty('--mouse-y', `${y}px`);
        });
        
        backToTopButton.addEventListener('mouseleave', (e) => {
            const mouseEvent = e as MouseEvent;
            const rect = backToTopButton.getBoundingClientRect();
            const x = mouseEvent.clientX - rect.left;
            const y = mouseEvent.clientY - rect.top;
            
            // Set the exit position for the circle to shrink from
            backToTopButton.style.setProperty('--mouse-x', `${x}px`);
            backToTopButton.style.setProperty('--mouse-y', `${y}px`);
            
            // Add a class to trigger the shrink animation
            backToTopButton.classList.add('btn--shrinking');
            
            // Remove the class and reset after animation completes
            setTimeout(() => {
                backToTopButton.classList.remove('btn--shrinking');
                backToTopButton.style.removeProperty('--mouse-x');
                backToTopButton.style.removeProperty('--mouse-y');
            }, 400); // Match the transition duration
        });
    }
</script>